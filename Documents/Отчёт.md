## Введение.
Здравствуйте, я Кардаильский Алексей, работающий над этим проектом и сейчас я проведу вам экскурсию по этому проекту.
Я Выбрал тему расчёта бюджета, потому что эта тема мне наиболее понятна и интересна, а так же она имеет сложность 3/3.
### В этой экскурсии вы прочтёте:

1. Предметная область.

2. Функционал проекта.

	2.1. О регестрации и входе в сеть.

	2.2. О самом приложении.
 
3. Описание программного кода.

	3.1. Главная страница.

	3.2. Регистрация.

	3.3. Вход в сеть.

	3.4. Приложение.
 
4. Заключение.

 	4.1. Список использованных источников.
 
	4.2. Скриншоты частей сайта.
## Предметная область:
Деньги не бесконечны.
Приложение показывает, сколько стоит потратить сегодня, а сколько оставить на завтра.
Считает новый бюджет на ходу, пока вы вводите трату.
Приложение показывает правду: если потратите слишком много сегодня, уменьшится сумму на завтра.
## Функционал проекта:
### О регестрации и входе в сеть:
Проект имеет систему регистрации и входа в сеть.
Логины и пароли находятся в Локальном хранилище.
Существует возможность выйти и из аккаунта, а так же удалить аккаунт.
При нажатии на "Выйти из аккаунта.", вас перенесёт на главную страницу сайта.
Что бы полностью защитить ваш аккаунт вам нужно будет так же выйти с сайта полностью.
При нажатии на "Удалить аккаунт.", вас так же перенесёт на главную страницу, но при этом ещё совершатся следующие действия:

1.Ваш логин и пароль удалятся из Локального хранилища и соответственно со всего сайта.

2.Удалятся все поля которые были заполнены вами.

Когда вы создаёте аккаунт при уже имеющимся аккаунте произойдут все те же действия, что и при кнопке "Удалить аккаунт.",
но вместо удаления вашего логина и пароля они заменятся на логин и пароль от вашего нового аккаунта.
По этому перед созданием аккаунта вам вылетит предупреждение.
### О самом приложении:
Для начала пользователю нужно ввести бюджет, валюту, и кол-во дней на которые нужно растянуть деньги.
Пока пользователь это не сделает будет вылетать ошибка.
За тем появится норма которую вам стоит потратить завтра.
После этого вы сможете ввести сумму которую растратели за день.
Это поле вы будете должны заполнять каждый день.
Если вы потратите меньше этой суммы, вам вылетит поздравление и ваша норма на завтра увеличется.
Соответственно если вы потратите всё накопленное за прошлый день, то ваша норма на завтра вернётся в изначальную.
Если вы потратите больше этой суммы, вам вылетит предупреждение и ваша норма на завтра уменьшится.
Соответственно если вы потратите меньше это суммы, то ваша норма на завтра вернётся в изначальную и при этом увеличется.
Если вы потратите ровно эту сумму, вам так же вылетит поздравление, но ваша норма на завтра останется на месте.
## Описание программного кода:
Финальный сайт разделён на 4 части:

1)Главная страница.

2)Регистрация.

3)Вход в сеть.

4)Приложение.

По порядку о каждой части.
### Главная страница:
Это самая простая по созданию страница.

Она встречает пользователя и пользователь ознокамливается с функционалом сайта.

Здесь пользователь понимает нужен ему этот сайта или нет.

Из этого сайта видут 3 дороги в 2 места.

2 дороги идут во 2 часть - регистрацию:

1- ссылка которая меняет цвет на более светлый.

2- кнопка которая также меняет цвет, но медленно.

И 1 дорога ведёт в 3 часть - вход в сеть:

3- ссылка которая меняет цвет на более светлый.

Для кнопки используются такие свойства CSS как:

{

    margin-left: 35%; <--- внешний отступ слева направо 35%.
    
    margin-top: 5%; <--- внешний отступ сверху вниз 5%.
    
    width: 30%; <--- ширина 30%.
    
    height: 80px; <--- высота 80 пикселей.
    
    border-radius: 3px; <--- радиус 3 пикселя.
    
    background-color: #ffa500; <--- задний фон этого цвета(#ffa500).
    
    border: 1px solid #e2dc16; <--- рамка 1 пиксель сплошного этого цвета(#e2dc16).
    
    outline: none; <--- убирает цвет, стиль и ширину внешней границы.
    
    box-shadow: 0 0 40px #0400ff; <--- делает тень 40 пикселей этого цвета(#0400ff).
    
    cursor: pointer; <--- при наведении делает мышку лапкой как при наведении на ссылки.
    
    font-size: 30px; <--- размер символов 30 пикселей.
    
    color: blue; <---  цвет символов голубой.
    
  }
  
  { <---  по наведению .

    background: #ffc300; <--- задний фон этого цвета(#ffc300).
 
    transition: width 2s, height 2s, background-color 2s, transform 4s;	<--- выполняет вышеперечисленные параметры 4 секунды.
    
  }

### Регистрация:
Это 2 по сложности страница.

Здесь уже есть алгоритм.

Здесь есть 3 проверки:

1- проверка на заполнение полей.
(Если заполнены не все поля-вылетит ошибка "Не все обязательные поля были заполнены.".)

2- проверка на 1 элемент.
(Если 1 элемент - пробел-вылетит ошибка "В строках 1 элемент не должен быть пробелом.".)

3- проверка на поля с паролями.
(Если пароли не совпадают-вылетит ошибка "Пароли не совпадают.".)

Соответственно если эти 3 проверки не выдали вам ошибки, т.е. вы ввели всё верно, то в Локальное хранилище добавится ваш логин и пароль.
Так же вы перейдёте в 4 часть - Приложение.

Вот полный алгоритм с пояснениями для каждой строки:
{

	'use strict' <--- находит ошибки в коде.
	let login = document.getElementById('text-1'); <--- создаёт переменную и кладёт в неё строку с логином.
	let password = document.getElementById('text-2'); <--- создаёт переменную и кладёт в неё строку с паролем.
	let password2 = document.getElementById('text-3'); <--- создаёт переменную и кладёт в неё строку с подтверждением пароля.
	let button = document.getElementById('button'); <--- создаёт переменную и кладёт в неё кнопку
	alert("Внимание!\nЕсли вы при уже имеющимся аккаунте создадите новый, то старый аккаунт полностью удалится и заменится на этот."); <---  выводит  предупреждение.
	button.addEventListener('click', function(){ <--- воспроизводит нижеперечисленные действия по нажатию кнопки.
	let a = document.getElementById('text-1').value; <--- создаёт переменную и кладёт в неё значение строки с логином.
	let a1 = document.getElementById('text-2').value; <--- создаёт переменную и кладёт в неё значение строки с паролем
	let a2 = document.getElementById('text-3').value; <--- создаёт переменную и кладёт в неё значение строки с подтверждением пароля.
	if(login.checkValidity()&password.checkValidity()&password2.checkValidity()){  <--- проверяет все ли обязательные поля заполнены.
	if(a[0]!=" "&a1[0]!=" "&a2[0]!=" "){ <--- проверяет не является ли 1 символ пробелом.
	if(a1 === a2){ <--- проверяет равны ли пароли
	localStorage.setItem('login3965', a); <--- создаёт логин в Локальном хранилище.
	localStorage.setItem('password3965', a1); <--- создаёт пароль в Локальном хранилище.

	localStorage.removeItem('date'); <--- удаляет 1 из элементов 4 части - Приложение.
	localStorage.removeItem('select'); <--- удаляет 1 из элементов 4 части - Приложение.
	localStorage.removeItem('text'); <--- удаляет 1 из элементов 4 части - Приложение.
	localStorage.removeItem('pre-last'); <--- удаляет 1 из элементов 4 части - Приложение.
	localStorage.removeItem('today'); <--- удаляет 1 из элементов 4 части - Приложение.
	localStorage.removeItem('last'); <--- удаляет 1 из элементов 4 части - Приложение.

	location.href = '../HTML/version 1.0 (3).html'; <--- перемещает пользователя в 4 часть - Приложение.
	}
	else <--- если пароли не совпадают
	{
		alert("Пароли не совпадают.") <--- выводит ошибку, "Пароли не совпадают.".
	};
	}
	else <--- если 1 элемент- пробел
	{
	alert("В строках 1 элемент не должен быть пробелом.") <--- выводит ошибку, "В строках 1 элемент не должен быть пробелом.".
	}
	}
	else <--- если не все поля заполнены
	{
		alert("Не все обязательные поля были заполнены.") <--- выводит ошибку, "Не все обязательные поля были заполнены.".
	};
	});

}

Для кнопок используются такие свойства CSS как:

{ <---  всегда.

	width: 200px; <--- ширина 200 пиксилей.
	
	height: 33px; <--- высота 33 пикселя.
	
	border-radius: 7px; <--- радиус 7 пикселей.
	
	color: #35cece; <--- цвет символов (#35cece).
	
	margin-left: 33%; <--- внешний отступ слева направо 33%.
	
	margin-top: 3%; <--- внешний отступ сверху вниз 3%.
	
	background-color: #4b4ee5; <--- цвет заднего фона (#4b4ee5).
	
	outline: none; <--- убирает цвет, стиль и ширину внешней границы.
	
	box-shadow: 0 0 20px #0004ff; <--- тень 20 пикселей этого цвета (#0004ff).
	
	border: 1px solid #9347e5; <--- рамка 1 пиксель сплошного этого цвета (#9347e5).
	
	cursor: pointer; <--- при наведении делает мышку лапкой как при наведении на ссылки. 
	
	font-weight: bold; <--- насыщенность шрифта жирный.
	
}
{ <---  по нажатию.

	background-color: #0700ed; <--- задний фон этого цвета (#0700ed).
	
	color: white; <--- цвет символов белый.
	
}
{ <---  по наведению.

	box-shadow: 0 0 20px #c7f400; <--- тень 20 пикселей этого цвета (#c7f400).
	
}

Для текстовых полей используются такие свойства CSS как:

{ <---  всегда.

	width: 400px; <--- ширина 400 пикселей.

	height: 33px; <--- высота 33 пикселя.

	border-radius: 3px; <--- радиус 3 пикселя.

	margin-left: 33%; <--- внешний отступ слево направо 33%.

	margin-top: 1%; <--- внешний отступ сверху вниз 1%.

	border: 1px solid blue; <--- рамка 1 пиксель сплошный синий.

	text-align: center; <--- текст в центре.

	display: block; <--- блочный элемент.

	box-shadow: 0 0 30px #00ff5d; <--- тень 30 пикселей этого цвета(#00ff5d).

	color: #4b4ee5; <--- цвет символов (#4b4ee5).

	font-weight: bold; <--- насыщенность шрифта жирный.
					
}

Из этого сайта идут так же 3 дороги:

1- ссылка "Уже есть аккаунт?". Как не трудно было догадаться она ведёт в 3 часть - вход в сеть.

2- кнопка "Вернуться на главную страницу". Она ведёт в 1 часть - Главная страница.

3- кнопка "Создать аккаунт.". Если все условия были выполнены верно, то кнопка перенесёт вас в 4 часть - Приложение.
Об этой части мы поговорим чуть позже.
### Вход в сеть.
Это 3 по сложности страница.

Здесь так же есть алгоритмы, но они проще чем во 2 части.

Здесь так же как и в прошлой части есть 3 проверки:

1 проверка, все ли данные введены.
(Если нет, то вылетит ошибка"Были введены не все данные.".)

2 проверка, существует ли ваобще аккаунт на этом компьютере.
(Если не существует, то вылетит ошибка"На этом компьютере аккаунта не существует.Создайте его по ссылке ниже.".)

3 проверка - на правильность логина и пароля.
(Если пароль или логин были введены не верно, то вылетит ошибка"Был введён неверный логин или пароль.".)

Вот полный алгоритм с пояснениями для каждой строки:
{

	'use strict' <--- находит ошибки в коде
	let text1 = document.getElementById('text-1'); <--- создаёт переменную и кладёт в неё 1 текстовую строку.
	let text2 = document.getElementById('text-2'); <--- создаёт переменную и кладёт в неё 2 текстовую строку.
	let button = document.getElementById('button'); <--- создаёт переменную и кладёт в неё кнопку.

	button.addEventListener('click', function(){ <--- по нажатию кнопки будут происходить нижеперечисленные действия.
	let a = document.getElementById('text-1').value; <--- создаёт переменную и кладёт в неё значение 1 текстовой строки.
	let a1 = document.getElementById('text-2').value; <--- создаёт переменнную и кладёт в неё значение 2 текстовой строки.

	let login = localStorage.getItem('login3965'); <--- создаёт переменную и кладёт в неё логин.
	let password = localStorage.getItem('password3965'); <--- создаёт переменную и кладёт в неё пароль.

	if(text1.checkValidity()&text2.checkValidity()){ <--- проверка, все ли обязательные поля были заполнены.

	if(login!=undefined){ <--- проверка, существует ли аккаунт на этом компьютере.

	if(login!=a|a1!=password){ <--- проверка, правильности пароля и логина.

	alert("Был введён неверный логин или пароль.") <--- выводит ошибку, "Был введён неверный логин или пароль.".
	}

	else <--- если и пароль и логин были введены верно
	{	
	location.href = '../HTML/version 1.0 (3).html'; <--- переносит вас в 4 часть - Приложение
	};

	}

	else <--- если аккаунта не существует на этом компьютере
	{
	alert("На этом компьютере аккаунта не существует.\nСоздайте его по ссылке ниже."); <--- выводит ошибку, "На этом компьютере аккаунта не существует.\nСоздайте его по ссылке ниже.".
	};		
	}

	else <--- если не обязательные поля были заполнены
	{
	alert("Были введены не все данные.") <--- выводит ошибку, "Были введены не все данные.".
	};
	});

}

Кнопки имеют следующие свойства CSS:

{ <--- всегда.

	width: 200px; <--- ширина 200 пикселей.
	
	height: 33px; <--- высота 33 пикселя.
	
	border-radius: 7px; <--- радиус 7 пикселей.
	
	color: #35cece; <--- цвет (#35cece).
	
	margin-top: 3%; <--- внешний отступ сверху вниз 3%.
	
	margin-left: 33%; <--- внешний отступ слева направо.
	
	background-color: #4b4ee5; <--- цвет заднего фона (#4b4ee5).
	
	outline: none; <--- убирает цвет, стиль и ширину внешней границы.
	
	box-shadow: 0 0 20px #0004ff; <--- тень 20 пикселей этого цвета (#0004ff).
	
	border: 1px solid #9347e5; <--- рамка 1 пиксель сплошный этот цвет (#9347e5).
	
	cursor: pointer; <--- при наведении делает мышку лапкой как при наведении на ссылки.
	
	font-weight: bold; <--- насыщенность шрифта жирный.
}

{ <--- по нажатию.

	background-color: #0700ed; <--- цвет заднего фона (#0700ed).
	
	color: white; <---  цвет белый.
}

{ <--- при наведении.

	box-shadow: 0 0 20px #c7f400; <--- тень 20 пикселей этого цвета (#c7f400).
}

Для текстовых строк используются следующие свойства CSS:

{

	width: 400px; <--- ширина 400 пикселей.
	
	height: 33px; <--- высота 33 пикселя.
	
	border-radius: 3px; <--- радиус 3 пикселя.
	
	margin-left: 33%; <--- внешний отступ слева направо 33%.
	
	margin-top: 1%; <--- внешний отступ сверху вниз 1 пиксель.
	
	border: 1px solid blue; <--- рамка 1 пиксель сплошный синий цвет.
	
	text-align: center; <---  текст в центре. 
	
	display: block; <--- блочный элемент.
	
	box-shadow: 0 0 30px #00ff5d; <--- тень 30 пикселей.
	
	color: #4b4ee5; <--- цвет (#4b4ee5).
	
	font-weight: bold; <--- насыщенность шрифта жирный.
}

Из этого сайта аналогично идут 3 дороги:

1- ссылка "Ещё нет аккаунта?". Как не трудно было догадаться она ведёт во 2 часть - Регистрация.

2- кнопка "Вернуться на главную страницу". Она ведёт в 1 часть - Главная страница.

3- кнопка "Войти в сеть.". Если все условия были выполнены верно, то кнопка перенесёт вас в 4 часть - Приложение. Об этой части мы поговорим чуть позже.
### Приложение.
Это самая сложная часть этого сайта.

Здесь есть 7 проверок:

1- проверка, были ли введены какие-то данные ранее.
(Если да, то текущие данные заменятся на старые)

2- проверка, были введены буквы или нет.
(Если были, то вылетит ошибка"Были введены числа или не было введно ничего.Пожалуйста введите только цифры.".)

3- проверка, заданы ли верхние параметры.
(Если пользователь захочет ввести нижние параметры до верхних, то вылетит ошибка"Для начала задайте верхние параметры.".)

4- проверка, закончились ли дни на которые расчитывались деньги.
(Если да, то вылетит поздравление и все данные обнулятся, если нет, то алгоритм продолжится)

5- проверка, за день было потрачено меньше нормы или нет.
(Если да, то вылетит поздравление и ваша норма на завтра увеличется.)

6- проверка, за день было потрачено больше нормы или нет.
(Если да, то вылетит предупреждение и ваша норма на завтра уменьшится.)

7- проверка, за день была потрачена ровно норма или нет.
(Если да, то вылетит поздравление, но ваша норма на завтра останется прежней.)

Вот полный алгоритм с пояснениями для каждой строки:

{

	'use strict' <--- находит ошибки в коде.
	let button = document.getElementById("button-2"); <--- создаёт переменную и кладёт туда 1 кнопку.
	let button2 = document.getElementById("button-3"); <--- создаёт переменную и кладёт туда 2 кнопку.
	let button3 = document.getElementById("button-4"); <--- создаёт переменную и кладёт туда 3 кнопку.
	let select = document.getElementById("select"); <--- создаёт переменную и кладёт туда select.
	let text  = document.getElementById("text-1"); <--- создаёт переменную и кладёт туда 1 текстовую строку.
	let text2 = document.getElementById("text-2"); <--- создаёт переменную и кладёт туда 2 текстовую строку.
	let date = document.getElementById("date"); <--- создаёт переменную и кладёт туда 3 текстовую строку.
	let div = document.getElementById("div1"); <--- создаёт переменную и кладёт туда "div".
	let c; <--- создаёт пустую переменную.
	let c1; <--- создаёт пустую переменную.
	let d1; <--- создаёт пустую переменную.
	let b; <--- создаёт пустую переменную.
	let b1; <--- создаёт пустую переменную.
	let b2; <--- создаёт пустую переменную.

	let h1 = document.createElement("h1"); <--- создаёт переменную и кладёт в неё элемент HTML (h1).

	let l = localStorage.getItem("date"); <--- создаёт переменную и кладёт туда date из Локального хранилища.
	if(l!=undefined){ <--- если "l" чему-то равно, то произойдут нижеперечисленные действия.
		b1 = l; <--- присваивает переменной "b1" значение переменной l.
		date.value = l; <--- присваивает значению date значение переменной l.
	};
	let l1 = localStorage.getItem("select"); <--- создаёт переменную и кладёт туда select из Локального хранилища.
	if(l1!=undefined){ <--- если "l1" чему-то равно, то произойдут нижеперечисленные действия.
		select.value = l1; <--- присваивает значению select значение переменной l1.
	};
	let l2 = localStorage.getItem("text"); <--- создаёт переменную и кладёт туда text из Локального хранилища.
	if(l2!=undefined){ <--- если "l2" чему-то равно, то произойдут нижеперечисленные действия.
		b = l2; <--- присваивает переменной "b "значение переменной l2.
		text.value = l2; <--- присваивает значению текстовой строки значение переменной l2.
	};
	let l3 = localStorage.getItem("pre-last"); <--- создаёт переменную и кладёт туда pre-last из Локального хранилища.
	if(l3!=undefined){ <--- если l3 чему-то равно, то произойдут нижеперечисленные действия.
		c1 = l3; <--- присваивает переменно "c1" значение переменной l3.
		c1 = +c1; <--- изменяет тип переменной "c1" с String на Number.
	};
	let l4 = localStorage.getItem("today"); <--- создаёт переменную и кладёт туда today из Локального хранилища.
	if(l4!=undefined){ <--- если l4 чему-то равно, то произойдут нижеперечисленные действия.
		b2 = l4; <--- присваивает переменной "b2" значение l4.
		b2 = +b2; <--- изменяет тип переменной "b2" с String на Number.
		text2.value = l4; <--- присваивает значению текстовой строки значение переменной l4.
	};
	let l5 = localStorage.getItem("last"); <--- создаёт переменную и кладёт туда last из Локального хранилища.
	if(l5!=undefined){ <--- если l5 чему-то равно, то произойдут нижеперечисленные действия.
		c = l5; <--- присваивает переменной "c" значение l4.
		c = +c;; <--- изменяет тип переменной "c" с String на Number.
	};
	if(c!=undefined&select.value!=undefined){ <--- если у переменной "c" есть значение и значение selectа не равно undefined то произойдут нижеперечисленные действия.
		div.appendChild(h1).innerHTML = "Завтра вам стоит потратить не больше " +c +select.value; <--- создаёт заголовок 1 уровня и помещает в него следующие элементы: "Завтра вам стоит потратить не больше " +c +select.value.
	};

	function isDigit(str) { <--- создаёт функцию с название isDigit
		return str && !/[^\d]/.test(str); <--- Проверяет строку на наличие букв, если есть то вылетает false, если нет то вылетает true.
	};

	button3.addEventListener('click', function(){ <--- по нажатию кнопки "button3" произойдут нижеперечисленные действия.
		localStorage.removeItem('date'); <--- из Локального хранилище удалится date.
		localStorage.removeItem('select'); <--- из Локального хранилище удалится select.
		localStorage.removeItem('text'); <--- из Локального хранилище удалится text.
		localStorage.removeItem('pre-last'); <--- из Локального хранилище удалится pre-last.
		localStorage.removeItem('today'); <--- из Локального хранилище удалится today.
		localStorage.removeItem('last'); <--- из Локального хранилище удалится last.
		localStorage.removeItem('login3965'); <--- из Локального хранилище удалится login3965.
		localStorage.removeItem('password3965'); <--- из Локального хранилище удалится password3965.
		location.href = 'version 1.0 (1).html'; <--- переносит пользователя на 1 часть - Главная страница.
	});

	button.addEventListener('click', function(){ <--- по нажатию кнопки "button" произойдут нижеперечисленные действия.

		if((isDigit(text.value)) === true&(isDigit(date.value)) === true){ <--- если в 2 текстовых строках нет букв, то произойдут нижеперечисленные действия.
			let a = text.value; <--- создаёт переменную "a" и кладёт в неё значение текстовой строки.
			b = +a; <--- присваивает переменной "b" значение переменной a и меняет тип этой переменной с String на Number.
			localStorage.setItem('text', b) <--- создаёт ключ "text" в Локальном хранилище и кладёт в него переменную "b".
			let d = date.value;  <--- создаёт переменную "d" и присваивает ей значение текстовой строки "date". 
			b1 = +d;  <--- присваивает переменной "b1" значение переменной "d" и меняет её тип с String на Number.
			localStorage.setItem('date', b1); <--- создаёт ключ "text" в Локальном хранилище и кладёт в него переменную "b1".	
			c = b/b1;  <--- присваивает переменной "c" значение "b/b1".
			localStorage.setItem('last', c); <--- создаёт ключ "last" в Локальном хранилище и кладёт в него переменную "c".
			c1 = c; <--- присваивает переменной "c1" значение переменной "с".
			localStorage.setItem('pre-last', c1); <--- создаёт ключ "pre-last" в Локальном хранилище и кладёт в него переменную "c1".
			div.appendChild(h1).innerHTML = "Завтра вам стоит потратить не больше " +c +select.value; <--- создаёт заголовок 1 уровня и помещает в него следующие элементы: "Завтра вам стоит потратить не больше " +c +select.value.
		}
		else <--- если в 2 текстовых строках есть буквы, то произойдут нижеперечисленные действия.
		{
			alert("Были введены числа или не было введно ничего.\nПожалуйста введите только цифры.") <--- выводит ошибку.
		};

	});

	button2.addEventListener('click', function(){ <--- по нажатию кнопки "button2" произойдут нижеперечисленные действия.
		if(div.appendChild(h1).innerHTML === ""){ <--- если div.appendChild(h1).innerHTML ничему не равен, то произойдут нижеперечисленные действия.
			alert("Для начала задайте верхние параметры."); <--- выводит ошибку "Для начала задайте верхние параметры.".
		}
		else <--- если div.appendChild(h1).innerHTML равен чему-либо, то произойдут нижеперечисленные действия.
		{	
			if((isDigit(text2.value)) === true){ <--- если в текстовой строке "text2" нет цифр, то произойдут нижеперечисленные действия.
				b1=b1-1; <--- переменной "b1" присваивается значение "b1-1".
				date.value = b1; <--- значение текстовой строки "date" становится равно значение переменной "b1".
				localStorage.setItem('date', b1); <--- создаёт ключ "date" в Локальном хранилище и кладёт в него переменную "b1".
				if(b1>0){ <--- если значение переменной "b1" меньше нуля, то произойдут нижеперечисленные действия.
					let a1 = text2.value; <--- создаёт переменнную "a1" и присваивает ей значение текстовой строки "text2".
					b2 = +a1; <--- присваивает переменной "b2" значение переменной "a1" и меняет тип этой переменной с String на Number.
					c = +c; <--- меняет тип переменной "c" с String на Number.
					localStorage.setItem('today', b2); <--- создаёт ключ "today" в Локальном хранилище и кладёт в него переменную "b2".
					if(c<b2){ <--- если значение переменной "c" меньше значения переменной "b2", то произойдут нижеперечисленные действия.
						d1 = c-b2; <--- присваивает переменной "d1" значение "c-b2".
						c = c1; <--- присваивает переменной "c" значение переменной "c1".
						localStorage.setItem('last', c); <--- создаёт ключ "last" в Локальном хранилище и кладёт в него переменную "c".
						c = c+d1; <--- присваивает переменной "c" значение "c+d1".
						localStorage.setItem('last', c); <--- создаёт ключ "last" в Локальном хранилище и кладёт в него переменную "c".
						alert("Увы! Вы потратили больше нормы на " +(-d1) +select.value); <--- выводит передупреждение, "Увы! Вы потратили больше нормы на +(-d1) +select.value".
						alert("Теперь ваша дозволенная трата на завтра уменьшится на " +(-d1) +select.value); <--- выводит передупреждение, "Теперь ваша дозволенная трата на завтра уменьшится на " +(-d1) +select.value.
						div.appendChild(h1).innerHTML = "Завтра вам стоит потратить не больше " +c +select.value; <--- создаёт заголовок 1 уровня и помещает в него следующие элементы: "Завтра вам стоит потратить не больше " +c +select.value.
					}
					else <--- в другом случае.
					if(c>b2){ <--- если значение переменной "c" больше значение переменной "b2", то произойдут нижеперечисленные действия.
						d1 = c-b2; <--- присваивает переменной "d1" значение "c-b2".
						c = c1; <--- присваивает переменной "c" значение переменной "c1".
						localStorage.setItem('last', c); <--- создаёт ключ "last" в Локальном хранилище и кладёт в него переменную "c".
						c = c+d1; <--- присваивает переменной "c" значение "c+d1".
						localStorage.setItem('last', c); <--- создаёт ключ "last" в Локальном хранилище и кладёт в него переменную "c".
						alert("Вау! Вы сэкономили " +d1 +select.value); <--- выводит поздравление, "Вау! Вы сэкономили " +d1 +select.value.
						alert("Теперь ваша дозволенная трата на завтра увеличится на " +d1 +select.value); <--- выводит поздравление, "Теперь ваша дозволенная трата на завтра увеличится на " +d1 +select.value.
						div.appendChild(h1).innerHTML = "Завтра вам стоит потратить не больше " +c +select.value; <--- создаёт заголовок 1 уровня и помещает в него следующие элементы: "Завтра вам стоит потратить не больше " +c 
					}
					else <--- в другом случае.
					if(c===b2){ <--- если значение переменной "c" равен значению переменной "b2", то произойдут нижеперечисленные действия.
						c = c1; <--- переменной "c" присваивается значение переменной "c1"
						alert("Так держать! Сегодня вы потратили не больше дозволенного.") <--- выводит поздравление, "Так держать! Сегодня вы потратили не больше дозволенного.".
						div.appendChild(h1).innerHTML = "Завтра вам стоит потратить не больше " +c +select.value; <--- создаёт заголовок 1 уровня и помещает в него следующие элементы: "Завтра вам стоит потратить не больше " +c. 
						localStorage.setItem('last', c); <--- создаёт ключ "last" в Локальном хранилище и кладёт в него переменную "c".

					}
				}
				else <--- иначе.
				if(b1===0){ <--- если значение переменно "b1" равно нулю, то произойдут нижеперечисленные действия.
					alert("Поздравляем, вы выжили."); <--- выводит финальное поздравление, "Поздравляем, вы выжили.".
					text.value = ""; <--- переменной "text" присваивается значение "".
					text1.value = ""; <--- переменной "text1" присваивается значение "".
					text2.value = ""; <--- переменной "text2" присваивается значение "".
					select.value = " руб."; <--- переменной "select" присваивается значение "руб.".
					div.appendChild(h1).innerHTML = ""; <--- создаёт заголовок 1 уровня и помещает в него следующие элементы: "Завтра вам стоит потратить не больше " +c.
					localStorage.removeItem('date'); <--- из Локального хранилище удалится date.
					localStorage.removeItem('select'); <--- из Локального хранилище удалится select.
					localStorage.removeItem('text'); <--- из Локального хранилище удалится text.
					localStorage.removeItem('pre-last'); <--- из Локального хранилище удалится pre-last.
					localStorage.removeItem('today'); <--- из Локального хранилище удалится today.
					localStorage.removeItem('last'); <--- из Локального хранилище удалится last.
				};
			}	
			else <--- иначе.
			{
				alert("Были введены числа или не было введно ничего.\nПожалуйста введите только цифры."); <--- выводит ошибку, "Были введены числа или не было введно ничего.Пожалуйста введите только цифры.".
			};
		};	
	});
	
}

Кнопки имеют следующие свойства CSS:

{ <--- всегда.

	height: 33px; <--- высота 33 пикселя.
	
	border-radius: 7px; <--- радиус 7 пикселей.
	
	color: #35cece; <--- цвета символов "#35cece".
	
	background-color: #4b4ee5; <--- цвет заднего фона "#4b4ee5".
	
	outline: none; <--- убирает цвет, стиль и ширину внешней границы.
	
	box-shadow: 0 0 20px #0004ff; <--- тень 20 пикселей этого цвета "#0004ff".
	
	border: 1px solid #9347e5; <--- рамка 1 пиксель сплошный цвет "#9347e5".
	
	cursor: pointer; <--- при наведении делает мышку лапкой как при наведении на ссылки.
	
	font-weight: bold; <--- насыщенность шрифта жирный.
	
	margin-left: 89%; <--- внешний отступ слева направо 89%.

}

{ <--- по нажатию

	background-color: #0700ed; <--- цвета заднего фона "#0700ed".
	color: white; <--- цвет символов белый.
	
}

{ <--- при наведении.

	box-shadow: 0 0 20px #c7f400; <--- тень 20 пикселей этого цвета "#c7f400".
	
}

Для текстовых строк используются следующие свойства CSS:

{

	width: 400px; <--- ширина 400 пикселей.
	
	height: 33px; <--- высота 33 пикселя.
	
	border-radius: 3px; <--- радиус 3 пикселя.
	
	margin-left: 33%; <--- внешний отступ слева направо 33%.
	
	margin-top: 1%; <--- внешний отступ сверху вниз 1 пиксель.
	
	border: 1px solid blue; <--- рамка 1 пиксель сплошный синий цвет.
	
	text-align: center; <---  текст в центре. 
	
	display: block; <--- блочный элемент.
	
	box-shadow: 0 0 30px #00ff5d; <--- тень 30 пикселей.
	
	color: #4b4ee5; <--- цвет (#4b4ee5).
	
	font-weight: bold; <--- насыщенность шрифта жирный.
	
}
## Заключение.
В процессе разработки проекта были решены следующие задачи:
•	сформулирована задача;
•	изучена предметная область;
•	рассмотрены аналогичные решения;
•	разработано техническое задание на разработку;
•	проведена экономическая оценка;
•	разработан программный продукт;
•	сформировано техническое описание разработки;
Таким образом, задачи, поставленные в рамках разработки дипломного проекта, выполнены. Все поставленные цели достигнуты.
### Список использованных источников.
1.Справочник JavaScript: [Электронный ресурс]URL:https://developer.mozilla.org/ru/docs/Web/JavaScript

2.Справочник JavaScript: [Электронный ресурс]URL:https://learn.javascript.ru

3.Справочник JavaScript: [Электронный ресурс]URL:https://javascript.ru

4.Реальный аналог моего сайта: [Электронный ресурс]URL:http://www.fuckgrechka.ru/tzlvt/

### Ниже приведены скриншоты использования интернет-ресурса, представлены основные функции и интерфейсы пользователя.

Главная страница интернет-ресурса, точка входа (рисунок П.1) 

Регистрация (рисунок П.2)

Вход в сеть (рисунок П.3)

Приложение (рисунок П.4)
